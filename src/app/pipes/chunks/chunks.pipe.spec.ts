/* tslint:disable:no-unused-variable */

import { ChunksPipe } from './chunks.pipe';

describe('Pipe: Chunkse', () => {
  it('create an instance', () => {
    let pipe = new ChunksPipe();
    expect(pipe).toBeTruthy();
  });
});
